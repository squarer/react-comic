var React = require('react');

const divStyle = {
  padding: '50px',
};

var Footer = React.createClass({
  render: function() {
    return (
      <div style={divStyle}></div>
    );
  }
});

module.exports = Footer;
