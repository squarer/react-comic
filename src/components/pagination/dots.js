var React = require('react');

var Dots = React.createClass({
  render: function() {
    return (
      <li className="dots">
        <span>...</span>
      </li>
    );
  }
});

module.exports = Dots;
